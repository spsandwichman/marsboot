module test;

fn store(arr: []mut int, index: int, value: int) -> int {
    let previous = arr[index];
    arr[index] = value;
    return previous;
}

fn access(foo: []let []let int) -> int {
    return foo[0][0];
}