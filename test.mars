module test;

// extern fun malloc(size: uint): ^mut;
// extern fun free(ptr: ^mut);
// extern fun realloc(ptr: ^mut, size: uint): ^mut;

def Foo = enum int {
    A, B, C, D, E, F
};

fun x(i: Foo): int {
    switch i
    case .A {
        return 10;
    }
    case .B, .C {
        return 11;
    }
    case .D {
        return 12;
    }

    return -1;
}