module test;

def string = []let u8;

// def Bar = struct {
//     arr: [10]string,
//     integer: int,
// };

def SOME_CONSTANT = 100;
mut foo: [SOME_CONSTANT]int;

fun sum_array(array: []let int): int {
    mut sum = 0;
    for i in 0 ..< array.len {
        sum += array[i];
    }
    return sum;
}