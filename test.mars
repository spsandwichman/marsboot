module test;

// fun sum_array(array: []let int): int {
//     mut sum = 0;
//     for i in 0 ..< array.len {
//         sum += array[i];
//     }
//     return sum;
// }

def Ptr = ^let Ptr;

def Bar = Ptr;

fun deref(p: Ptr): Ptr {
    return p^^^^^^;
}