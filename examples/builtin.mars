// this file wont compile, using "builtin" as a module name is not allowed.
// just for documentation
module builtin;

def arch: enum {
    C,   // compiling to 64-bit c
} = ---; // defined by compiler

def system: enum {
    Unknown,
    Freestanding,
    Windows,
    Linux,
} = ---;